jwt:
  secret: xxx= # needs to be replaced with a secure random string from environment variable
  expiration: 86400000

spring:
  application:
    name: demo
  docker:
    compose:
      file: backend/compose.yaml
      lifecycle-management: start-only
